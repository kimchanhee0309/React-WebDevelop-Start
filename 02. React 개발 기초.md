# 1. JSX

<details>
  <summary>(1) JSX란?</summary>

  * JSX = **JavaScript 속에서 HTML처럼 쓸수 있는 문법**
</details>

<details>
  <summary>(2) JSX의 3가지 기본 규칙</summary>

  * **규칙 1: 반드시 하나의 태그로 감싸기**
    ```JavaScript
    // 에러! 태그 두 개가 나란히 있음
    return (
      <h1>제목<h1>
      <p>내용</p>
    );

    // OK - div로 감쌌음
    return (
      <div>
        <h1>제목</h1>
        <p>내용</p>
      </div>
    );

    // 감싸는 div가 싫다면? 프래그먼트를 통해 해결 가능
    ```
    
  * **규칙 2: HTML 속성은 camelCase로**
    * JSX와 HTML은 비슷하지만, **속성 이름이 약간 다름**
      
  |HTML|JSX|
  |---|---|
  |`class`|`className`|
  |`for`|`htmlFor`|
  |`onclick`|`onClick`|
  |`onchange`|`onChange`|
  |`tabindex`|`tabIndex`|

  ```JavaScript
  // HTML 스타일
  <div class="container" onclick="handleClick()">

  // JSX 스타일
  <div className="container" onClick={handleClick}>

  // class는 JS에서 이미 예약어이기 때문에, 겹치지 않게 바꾸는 것
  ```

  * **규칙 3: 모든 태그는 반드시 닫기**
    ```JavaScript
    // X, HTML에선 가능했지만 JSX에선 에러
    <br>
    <img src="photo.jpg">
    <input type="text">

    // O, 셀프 클로징(/>)으로 닫기
    <br />
    <img src="photo.jpg" />
    <input type="text" />
    ```
</details>

<details>
  <summary>(3) JSX에서 주석 쓰기</summary>

  * JSX 안에선 주석을 `{/* */}` 형태로 씀
    ```JavaScript
    function App() {
      return (
        <div>
        {/* 이렇게 주석을 쓴다 */}
        <h1>제목</h1>

        {/*
          여러 줄 주석도
          이렇게 가능해요
        */}
        <p>내용</p>
      </div>
      };
    }
    ```
</details>

<br>

# 2. 프래그먼트(Fragment)

<details>
  <summary>(1) div로 감싸면 생기는 문제</summary>

  * **문제 1: 불필요한 DOM이 쌓임**
    * `<div>` 감싸기를 반복하면 실제 HTML 구조는 다음과 같이 됨
      ```JavaScript
      <div>
        <div>
          <div>
            <div>
              <h1>안녕</h1>
            </div>
          </div>
        </div>
      </div>
      ```
      * **의미 없는 `<div>` 껍데기가 잔뜩 쌓임**
      
  * **문제 2: 특정 HTML 구조에선 아예 에러**
    * `<table>` 같이 **규칙이 엄격한 구조**에선 `<div>`를 끼워넣으면 HTML이 깨짐
      ```JavaScript
      // 잘못된 구조
      function TableRow() {
        return (
          <div>    {/* 여기 div 때문에 테이블이 망가짐! */}
            <td>이름</td>
            <td>나이</td>
          </div>
        };
      }
      ```
      ```JavaScript
      <!-- 결과: HTML 표준 위반! -->
      <table>
	      <thead>
	        <tr>
	          <div>   ← table 안에 div가 들어갈 수 없어요!
	            <td>이름</td>
	            <td>나이</td>
	          </div>
	        </tr>
	      </thead>
      </table>
      ```
</details>


<details>
  <summary>(2) Fragment = 투명한 포장지</summary>

  * Fragment는 **투명한 포장지**임
  * 여러 물건을 **묶어주긴 하지만**, 실제 DOM에선 **보이지 않음**
  * `<div>`가 택배 박스라면, Fragment는 **랩으로 살짝 감싸는 느낌**
</details>


<details>
  <summary>(3) Fragment 쓰는 2가지 방법</summary>

  * **방법 1: 빈 태그 `<> </>`(가장 많이 씀)⭐**
    ```JavaScript
    function TableRow() {
      return (
        <>
          <td>이름</td>
          <td>나이</td>
        </>
      };
    }
    ```
  * **방법 2: `<React.Fragment>`(풀네임)**
    ```JavaScript
    import React from 'react';

    function TableRow() {
      return (
        <React.Fragment>
          <td>이름</td>
          <td>나이</td>
        </React.Fragment>
      };
    }
    ```

  * **✨ Fragment를 쓴 결과**
    ```JavaScript
    <!-- 이제 HTML 구조가 깨끗해요! -->
    <table>
      <tr>
        <td>이름</td>
        <td>나이</td>
      </tr>
    </table>
    ```
    * `<div>`도, Fragment도 **결과 HTML엔 흔적이 없음**
</details>


<details>
  <summary>(4) Fragment vs div - 언제 뭘 쓸까?</summary>

  |상황|추천|
  |---|---|
  |스타일(CSS)이나 클래스가 필요할 때|`div`|
  |id, onClick 등 속성이 필요할 때|`div`|
  |단순히 그룹화만 필요할 때|Fragment`<>`|
  |테이블, 리스트 등 HTML 구조가 엄격할 때|Fragment`<>`|

  * **간단한 판단 기준**
    * **이 감싸는 태그에 뭔가 필요한가?**
      * 스타일/속성이 필요해 -> `<div>`
      * 그냥 묶기만 하면 돼 -> `<> </>`

  * **⚠️ 예외: Key가 필요할 땐 `React.Fragment`**
    * **딱 한 가지 경우**에만 빈 태그 `<>` 대신 **풀네임 `<React.Fragment>`**를 써야 함
    * 바로 **리스트에 key가 필요할 때**임
      ```JavaScript
      // 빈 태그는 key를 받을 수 없음
      users.map(user => (
        <key={user.id}>  {/* 이렇게 못 씀! */}
          <h3>{user.name}</h3>
          <p>{user.email}</p>
        </>
      ))
      
      // key가 필요하면 풀네임 Fragment 사용
      users.map(user => (
        <React.Fragment key={user.id}>
          <h3>{user.name}</h3>
          <p>{user.email}</p>
        </React.Fragment>
      ))
      ```

  * **정리**
    
  |문법|용도|
  |---|---|
  |`<> </>`⭐|거의 모든 경우(권장)|
  |`<React.Fragment>`|key가 필요한 예외 상황|
  |`<div>`|스타일/속성이 필요할 때|
      
</details>

<br>

# 3. JSX에서 JavaScript 사용하기

<details>
  <summary>(1) 변수와 상수 - 기본 중의 기본</summary>

  ```JavaScript
  function UserCard() {
    const userName = '김철수';
    const userAge = 28;

    return (
      <div>
        <h2>{userName}</h2>
        <p>나이: {userAge}세</p>
      </div>
    };
  }

  // 가장 흔한 사용법
  // JSX 바깥에서 변수를 선언하고 안에서 꽂아 넣기
  ```
</details>

<details>
  <summary>(2) 객체 사용 - 점(.)으로 접근</summary>

  ```JavaScript
  function UserProfile() {
    const user = {
      name: '이영희',
      age: 32,
      job: '디자이너'
    };

    return (
      <div>
        <h2>{user.name}</h2>
        <p>{user.age}세, {user.job}</p>
      </div>
    );
  }
  ```
  * 객체의 속성은 **점 표기법(`user.name`)** 으로 접근
</details>

<details>
  <summary>(3) 함수 호출 - 복잡한 로직은 함수로</summary>

  ```JavaScript
  function Greeting() {
    function getGreeting(hour) {
      if (hour < 12) return '좋은 아침입니다';
      if (hour < 18) return '좋은 오후입니다';
      return '좋은 저녁입니다';
    }

    const currentHour = new Date().getHours();

    return <h1>{getGreeting(currentHour)}</h1>;
  }
  ```
  * **조건이 복잡하면 함수로 빼야함**
  * JSXsms **뭘 보여줄지**에 집중하고, **어떻게 계산할지**는 함수에 맡기는 게 깔끔함
</details>

<details>
  <summary>(4) 조건 표현식 - 삼항 연산자</summary>

  ```JavaScript
  function StatusBadge({ status }) {
    return (
      <span className={status === 'active' ? 'badge-green' : 'badge-gray'}>{status === 'active' ? '활성' : '비활성'}</span>
    );
  }
  ```
</details>

<details>
  <summary>(5) 배열 메서드 - map과 filter</summary>

  * 리스트를 화면에 뿌릴 때 가장 많이 쓰는 패턴
  * **기본 - `map` - 배열의 각 요소를 JSX로 변환**
    ```JavaScript
    function FruitList() {
      const fruits = [
        { id: 1, name: '사과' },
        { id: 2, name: '바나나' },
        { id: 3, name: '오렌지' }
      ];

      return (
        <ul>
          {fruits.map(fruit => (
            <li key={fruit.id}>{fruit.name}</li>
          ))}
        </ul>
      );
    }
    ```

  * **응용 - `filter` + `map`체이닝**
    ```JavaScript
    function UserList({ users }) {
      return (
        <ul>
          {users
            .filter(user => user.active)  // 활성 사용자만
            .map(user => (                // JSX로 변환
              <li key={user.id}>{user.name}</li>
            ))}
        </ul>
      );
    }
    ```
  * **⚠️ 안티패턴 #1: `key{index}`**
    ```JavaScript
    // X index를 key로 쓰면 안됨
    {fruits.map((fruit, index) => (
      <li key={index}>{fruit}</li>
    ))}
    ```
  * **개선: 고유한 ID를 key로**
    ```JavaScript
    // 각 항목의 고유 id를 key로
    {fruits.map(fruit => (
    	<li key={fruit.id}>{fruit.name}</li>
    ))}
    ```
</details>

<details>
  <summary>(6) 템플릿 리터럴 - 문자열 조합</summary>

	```JavaScript
	function ProductCard({ product }) {
		return (
			<div className={`product-card ${product.featured ? 'featured' : ''}`}>
				<h3>{product.name}</h3>
			</div>
		);
	}
	```
	
  * **⚠️ 안티패턴: 불필요한 템플릿 리터럴**
    ```JavaScript
    <p>{`₩${product.price.toLocaleString()}`}</p>
    <p>{`재고: ${product.stock}개`}</p>
    ```

  * **개선: JSX의 자연스러운 흐름 활용**
    ```JavaScript
    <p>₩{product.price.toLocaleString()}</p>
    <p>재고: {product.stock}개</p>
    ```
</details>

<details>
  <summary>(7) 꼭 피해야 할 패턴: 즉시 실행 함수(IIFE)</summary>

  * **안티패턴: JSX 안에서 IIFE**
    ```JavaScript
    function Dashboard({ score }) {
    	return (
    		<div>
    			<p>점수: {score}</p>
    			<p>등급: {
    				(() => {
    					if (score >= 90) return 'A';
    					if (score >= 80) return 'B';
    					if (score >= 70) return 'C';
    					return 'D';
    				})()
				}</p>
    		</div>
    	);
    }
    ```
    * **문제점**
	  * **읽기 지옥**: `(() => {})()` 괄호 지옥
      * **디버김 어려움**: 함수에 이름이 없어서 에러 추적이 힘듦
      * **재사용 불가**: 같은 로직을 다른 곳에서 쓸 수 없음

  * **개선: 함수로 분리**
    ```JavaScript
    function getGrade(score) {
    	if (score >= 90) return 'A';
    	if (score >= 80) return 'B';
    	if (score >= 70) return 'C';
    	return 'D';
    }

    function Dashboard({ score }) {
    	return (
    		<div>
    			<p>점수: {score}</P>
    			<P>등급: {getGrade(score)}</p>
    		</div>
    	);
    }
    ```
</details>
-*
<br>

# 4. 컴포넌트(Component)

<details>
  <summary>(1) 컴포넌트란?</summary>

* **한 줄 정의: 화면을 구성하는 독립적인 부품**
* **쇼핑몰 하나를 컴포넌트로 쪼개보면?**
  ```
  🛍️ 쇼핑몰 페이지
  ├── 📋 Header (로고 + 검색창 + 장바구니)
  ├── 📦 ProductList
  │   ├── 🃏 ProductCard (상품 1)
  │   ├── 🃏 ProductCard (상품 2)
  │   └── 🃏 ProductCard (상품 3)
  └── 📜 Footer
  ```
  * 각 부품(컴포넌트)는 **독립적**이고, **재사용 가능함**
  * `ProductCard` 하나를 잘 만들어두면, 상품이 100개든 1000개든 끼워넣으면 됨

  * **컴포넌트의 3대 장점**

	|장점|설명|
	|---|---|
	|**🔄 재사용성**|한 번 만들면 여러 곳에서 쓸 수 있음|
	|**🛠️ 유지보수**|버그 나면 해당 컴포넌트만 고치면 됨|
	|**👥 협업**|팀원들이 각자 다른 컴포넌트 담당 가능|

</details>

<details>
  <summary>(2) 컴포넌트 만들기</summary>

* **컴포넌트 = JSX를 반환하는 함수**
  ```JavaScript
  function Welcome() {
  	return <h1>환영합니다!</h1>;
  }
  ```
  
* **쓸 때는 HTML 태그처럼!**
  ```JSX
  function App() {
  	return (
  		<div>
  			<Welcome />		{/* ← 내가 만든 컴포넌트를 태그처럼! */}
  			<Welcome />	  {/* 재사용도 가능! */}
  			<Welcome />
  	);
  }
  ```
	
</details>

<details>
  <summary>(3) 절대 규칙: PascalCase로 시작하기</summary>

* 컴포넌트 이름은 **반드시 대문자**로 시작해야 함(타협 불가)
  ```JavaScript
  // O
  function Welcome() { }
  function UserProfile() { }
  function ProductCard() { }

  // X
  function welcome() { }				// React가 HTML 태그로 착각
  function userProfile() { }	  // 렌더링 안 됨
  ```
</details>

<details>
  <summary>(4) 컴포넌트 분리하기 - Before/After</summary>

* **Before: 한 파일에 전부(지옥)**
  ```JSX
  function App() {
  	return (
  		<div>
  			<header>
  				<h1>My Blog</h1>
  				<nav>
  					<a href="/">Home</a>
  					<a href="/about">About</a>
  				</nav>
  			</header>

  			<main>
  				<article>
  					<h2>게시물 제목</h2>
  					<p>게시물 내용...</p>
  				</article>
  			</main>

  			<footer>
  				<p>&copy; 2024 My Blog</p>
  			</footer>
			</div>
		);
  }
  ```
  
* **After: 역할별로 분리(천국)**
  ```JSX
  function Header() {
  	return (
  		<header>
  			<h1>My Blog</h1>
  			<nav>
  				<a href="/">Home</a>
  				<a href="/about">About</a>
  			</nav>
			</header>
  	);
  }

  function Article() {
  	return (
  		<article>
  			<h2>게시물 제목</h2>
  			<p>게시물 내용...<p>
  		</article>
  	);
  }

  function Footer() {
  	return (
  		<footer>
  			<p>&copy; 2024 My Blog</p>
  		</footer>
  	);
  }

  function App() {
  	return (
  		<div>
  			<Header />
  			<main><Article /></main>
  			<Footer />
  		</div>
  	);
  }
  ```
</details>

<details>
  <summary>(5) 파일로 분리하기 - 진짜 실무 스타일</summary>

* **폴더 구조**
  ```
  src/
	├── components/        ← 컴포넌트 모아두는 폴더
	│   ├── Header.jsx
	│   └── Footer.jsx
	├── App.jsx
	└── main.jsx
  ```

* **`src/components/Header.jsx`**
  ```JSX
  function Header() {
  	return (
  		<header>
  			<h1>My Blog</h1>
  		</header>
  	);
  }

  export default Header;
  ```

* **`src/components/Footer.jsx`**
  ```JSX
  function Footer() {
  	return (
  		<footer>
  			<p>&copy; 2024 My Blog</p>
  		</footer>
  	);
  }

  export default Footer;
  ```

* **`src.App.jsx`**
  ```JSX
  import Header from './components/Header';
  import Footer from './components/Footer';

  function App() {
  	return (
  		<div>
  			<Header />
  			<main><h2>메인 컨텐츠</h2></main>
  			<Footer />
  		</div>
  	);
  }

  export default App;
  ```
</details>

<details>
  <summary>(6) 폴더 구조 가이드(실무 스타일)</summary>

	```
	src/
	├── components/
	│   ├── common/           ← 버튼, 인풋 등 자주 쓰는 공통 부품
	│   │   ├── Button.jsx
	│   │   ├── Input.jsx
	│   │   └── Card.jsx
	│   ├── layout/           ← 페이지 레이아웃 부품
	│   │   ├── Header.jsx
	│   │   ├── Sidebar.jsx
	│   │   └── Footer.jsx
	│   └── features/         ← 특정 기능 전용 부품
	│       ├── UserProfile.jsx
	│       └── ProductList.jsx
	├── App.jsx
	└── main.jsx
	```
</details>

<br>

# 5. Props⭐️

* **학습목표**: 컴포넌트에 **데이터를 전달**해서, 같은 컴포넌트를 **다양하게 활용**
<details>
  <summary>(1) Props란?</summary>

* **Props** = **Prop**erties의 줄임말
* **한 줄 정의**: **부모 컴포넌트**가 **자식 컴포넌트**에게 전달하는 **데이터**
* **기본 예제**
  ```JSX
  // 자식 컴포넌트
  function Greeting(props) {
  	return <h1>안녕하세요, {props.name}님!</h1>;
  }

  // 부모 컴포넌트
  function App() {
  	return (
  		<div>
  			<Greeting name="홍길동" />
  			<Greeting name="김철수" />
  			<Greeting name="이영희" />
  		</div>
  	);
  }
  ```
  * **결과**
    ```
    안녕하세요, 홍길동님!
    안녕하세요, 김철수님!
    안녕하세요, 이영희님!
    ```
</details>

<details>
  <summary>(2) 구조 분해 할당 - 더 깔끔하게</summary>

* **기본 방식**
  ```JSX
  function UserCard(props) {
  	return (
  		<div>
  			<h2>{props.name}</h2>
  			<p>{props.age}세</p>
  			<p>{props.job}</p>
  	);
  }
  ```

* **구조 분해 할당(실무에선 이게 표준임)**
  ```JSX
  function UserCard({ name, age, job }) {
  	return (
  		<div>
  			<h2>{name}</h2>
  			<p>{age}세</p>
  			<p>{job}</p>
  		</div>
  	);
  }

  // 중괄호{}로 받으면 각 속성을 변수처럼 바로 쓸 수 있음
  ``` 
</details>

<details>
  <summary>(3) 다양한 타입의 Props 전달하기</summary>
</details>

<details>
  <summary>(4) Props 기본값 설정하기</summary>
</details>

<details>
  <summary>(5) Spread 연산자로 한 번에 전달하기</summary>
</details>

<details>
  <summary>(6) Props는 읽기 전용</summary>
</details>

<details>
  <summary>(7) 실무 네이밍 관례</summary>
</details>

<details>
  <summary>(8) 정리</summary>
</details>

<br>

# 6. children⭐️

<details>
  <summary>들어가기 전에</summary>
</details>

<details>
  <summary>(1) children이란?</summary>
</details>

<details>
  <summary>(2) children 받아쓰기</summary>
</details>

<details>
  <summary>(3) Props vs children - 언제 뭘 쓸까?</summary>
</details>

<details>
  <summary>(4) 실전 예제</summary>
</details>

<details>
  <summary>(5) children + 다른 props 조합</summary>
</details>

<details>
  <summary>(6) 자주 하는 안티패턴</summary>
</details>

<br>

# 7. State⭐️

<details>
  <summary>(1) State란?</summary>
</details>

<details>
  <summary>(2) useState 사용하기</summary>
</details>

<details>
  <summary>(3) 여러 State 함께 쓰기</summary>
</details>

<details>
  <summary>(4) 중요한 패턴: 함수형 업데이트</summary>
</details>

<details>
  <summary>(5) 토글 패턴</summary>
</details>

<details>
  <summary>(6) 자주 하는 안티패턴</summary>
</details>

<br>

# 8. 참조형 State⭐️

<details>
  <summary>(1) 참조형이란?</summary>
</details>

<details>
  <summary>(2) 불변성(Immutability) - 오늘의 절대 원칙</summary>
</details>

<details>
  <summary>(3) 객체 State 다루기</summary>
</details>

<details>
  <summary>(4) 배열 State 다루기 - 3대 패턴</summary>
</details>

<details>
  <summary>(5) 중첩된 객체 - 피해야 할 함정</summary>
</details>

<details>
  <summary>(6) 자주 하는 안티패턴</summary>
</details>

<br>

# 9. 컴포넌트 재사용하기

<details>
  <summary>(1) 반복은 개발자의 적!</summary>
</details>

<details>
  <summary>(2) 컴포넌트로 추출하기 - Before/After</summary>
</details>

<details>
  <summary>(3) 배열 데이터로 자동 렌더링</summary>
</details>

<details>
  <summary>(4) 범용 Button 컴포넌트 - 실전 설계</summary>
</details>

<details>
  <summary>(5) 좋은 재사용 컴포넌트의 3가지 조건</summary>
</details>

<details>
  <summary>(6) 자주 하는 안티패턴</summary>
</details>

<br>

# 10. React가 렌더링하는 방식

<details>
  <summary>(1) 리액트에서 말하는 "렌더링"이란?</summary>
</details>

<details>
  <summary>(2) 두 단계로 나뉨</summary>
</details>

<details>
  <summary>(3) 언제 렌더링 될까?</summary>
</details>

<details>
  <summary>(4) Virtual DOM - 똑똑한 비서</summary>
</details>

<details>
  <summary>(5) 배치 업데이트(Batching) - 영리한 모아 처리</summary>
</details>

<details>
  <summary>(6) 렌더링 실제로 관찰해보기</summary>
</details>

<details>
  <summary>(7) 주의해야 할 패턴</summary>
</details>

<br>

# 11. 인라인 스타일

<details>
  <summary>들어가기 전에 - React엔 스타일링 방법이 여러 개 있음</summary>
</details>

<details>
  <summary>(1) 인라인 스타일 기본 문법</summary>
</details>

<details>
  <summary>(2) 스타일 객체를 분리하기</summary>
</details>

<details>
  <summary>(3) 인라인 스타일의 진짜 강점: 동적 스타일</summary>
</details>

<details>
  <summary>(4) 인라인 스타일의 한계</summary>
</details>

<details>
  <summary>(5) 그래서 뭐로 연습?(명확한 가이드)</summary>
</details>

<br>

# 12. CSS 클래스네임

<details>
  <summary>(1) className 기본</summary>
</details>

<details>
  <summary>(2) 동적 클래스 조합</summary>
</details>

<details>
  <summary>(3) 조건부 클래스 3가지 패턴</summary>
</details>

<details>
  <summary>(4) 실무 꿀팁: clsx 라이브러리</summary>
</details>

<details>
  <summary>(5) CSS Modules - 파일별 스코프 격리(강력 추천⭐️)</summary>
</details>
